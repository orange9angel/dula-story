# Visual review — yuki_beat_ad

## V6（元素扩充，2026-09-16）

- 13.6 秒、720×1280、60 fps，816 帧。14 个状态、13 次人物变更与 V5 一致；
  音乐、混音、拍点网格不变（`mixed_v6.wav` 为 V5 母带拷贝）。
- 目检 `storyboard/v6/` 抽查帧：swipe 中帧/snap zoom 峰值帧（portrait_10）、
  霓虹夜街亮相帧（portrait_11）、字卡帧（portrait_16/18）、星空云海 +
  残影帧（portrait_35/39）。
- 场景跳切可读：三段场景同风格（平涂+发光几何），人物站位/机位不变，
  切点即重音；swipe 色块 0.3s 擦过，中段帧可见半屏旧场景半屏色块。
- snap zoom 峰值 1.116–1.130，回弹后精确归 1.0；峰值帧皇冠/贝雷帽未裁切。
- 字卡 130%→100% 砸下可读，位于裙摆高度、不挡脸；0.6s 停留后 0.15s 淡出，
  4.09 与 5.02 两张卡间隔 0.93s 不重叠。
- 残影分身在 dance 窗内 2 个（0.3/0.15 透明度），窗外为 0；静态帧呈
  头发/手臂边缘重影，动态读作残影。
- 目检修复一项：霓虹/星空段顶部「小雪 YUKI」标签深底不可读，改为按场景
  切换白/深色后重出检查帧确认。
- 逐次检查最大"攻击点→首个变更帧"误差 13.3ms；场景序列 candy→neon→star、
  snap 峰值/回弹、字卡显隐、swipe 方向、残影数量均经 `review_v6.py` 断言。
  最终 AAC 解码峰值 0.7728，无越界样本；专用竖屏入口无浏览器异常。
- 该结果只证明时序与元素实现，不证明音乐或审美达标；本轮没有人工听审。

## V3（人物变装修订，2026-09-13）

- 13.2 秒、720×1280、60 fps，792 帧。15 个状态，14 次人物变更。
- 目检 `storyboard/v3/review_1.jpg` 至 `review_4.jpg`，覆盖每次变化前一帧、
  首个落拍帧、保持帧和旋转中段；鞋尖擦边已通过旋转时扩大视野并重定中心修复。
- 服装存在可见几何差别：兔耳连帽裙、水手服与贝雷帽、黄色外套、皇冠长裙。
  墨镜在重音直接出现；同一服装还可切换噘嘴、闭眼笑、吐舌、眨眼等反应。
- 相机基本保持同一位置，避免景别变化掩盖人物变化；底部标签不盖鞋子。
- 逐次检查的最大“检测攻击点→首个变更帧”误差为 15ms；确认每次至少一个
  outfit/accessory/expression/gesture 字段发生变化，墨镜开关与实际可见性一致，
  替换服装最多显示一套。该结果只证明时序实现，不证明音乐或审美达标。
- 标准词汇检查 15 entries / 0 errors / 0 warnings；scene contract 通过。
  标准 dula-verify 对剧本镜像输出 15 张基础场景图，不能验证专用入口的服装与表情。
- 最终 AAC 解码峰值约 0.7563，无越界样本；专用竖屏入口无浏览器异常。
- 音频三选一和混音 A/B 均由音频模型辅助，C 与原始乐句分别胜出。视听模型仍
  认为同步感不足，与音频盲评存在分歧，完整负面评审保留于 model_av_review.txt。
  本轮没有人工听审或参考原片直接对照声明。

## V2（2026-09-13，试剪）

- `output/yuki_beat_ad_v2.mp4`：384 帧，720×1280，30 fps，12.8 秒。
- 目检 `storyboard/v2/review_1.jpg` 至 `review_3.jpg`：34 个采样覆盖切点前后、
  旋转中段、表情与结尾。闭眼弧线没有虹膜残留，惊讶嘴与笑嘴可辨；主旋转时
  角色完整入框，脸部特写允许边缘发梢裁切，眼睛和嘴保留。
- 主重音 4.595s，视频第 138 帧（4.600s）回正切笑脸；10 次切镜相对于检测攻击
  点的帧率量化误差 1.7–31.7ms。攻击检测不是人工节拍标注，听感仍需复核。
- 严格词汇检查：11 entries，0 errors，0 warnings。场景 contract 通过。
- 标准 dula-verify 对临时 V2 剧本镜像检查基础场景；最终表情和滚转由专用竖屏
  入口执行，最终视觉证据以 `storyboard/v2/` 为准。
- 最终 AAC 解码峰值 0.71592，越界样本 0，48kHz 双声道；浏览器错误 0。
- 配乐为本地缓存试选，新增重音前约 0.165s 音量停顿。没有新模型调用，未核实
  原生成记录，没有人工听审声明，也未通过参考网站校验取得视频作逐帧对照。
- 原版保留。本版验证重音、表情和镜头的配合，音乐审美与最终发布定稿待评审。

## V1

- 384 帧，720×1280 / 30 fps；七个动作镜头，末镜于 11.2 秒保持画面。
- 已检查 `storyboard/portrait_01.jpg` 至 `portrait_08.jpg` 及总览。
- 小雪全片角色一致；原有造型采用棚拍材质，未重新生成人脸或视频。
- 字卡背景渐变保持标题对比，近景底部文字有浅色卡片，避免深色裙子干扰阅读。
- 转身镜保持角色在画面中，近景保留头发与双眼，末镜留下完整品牌字卡。
- 角色抬升发生在两次鼓点之间，落地回到拍点；足底基线按原模型鞋底补偿。
- 简模的头发体积较大、手臂较短：挥手幅度有限，属于当前资产风格的限制。
- 标准 dula-verify 为横屏无文案检查；竖屏样片以 portrait 检查图为准。
- 已审核旧 inspector 对无对白角色和自定义运镜的误报，详见 README。
- 音频来源为现有程序合成 BGM，额外留出 AAC 编码峰值余量；本次没有人工听审声明。
